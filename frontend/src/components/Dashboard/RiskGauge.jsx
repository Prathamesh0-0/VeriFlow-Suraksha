import React from 'react';

// RiskGauge is merged into OverviewPanel — this is kept as a no-op
// to avoid breaking imports, but we don't render it separately anymore.
export default function RiskGauge() {
  return null;
}
